# Everything here is meant to be found.
User-agent: *
Allow: /

# Astro's sitemap integration emits this index at build time.
#
# NOTE: this path must match your deployment. The default below assumes the
# GitHub Pages *project* site (PUBLIC_BASE_PATH=/Student_registration).
# On a custom domain, change it to https://your-domain/sitemap-index.xml
Sitemap: https://tebitramson69-netizen.github.io/Student_registration/sitemap-index.xml
